let duration = document.getElementById('duration').value;

function calcHourMin(duration){
    let durList = []
    let hour = duration / 60;
    if(hour < 1){
        hour = 0;
    }
    let minutes = 
}
